node_modules/
dist/
.DS_Store
*.log
tsconfig.tsbuildinfo

# ローカルの作業計画ドキュメント
plan.md
plan.pdf

# AI アシスタント: 秘密情報とエージェントのセッションファイル
.env
data/agent-sessions/

# ユーザーごと storage 分離で作られる per-user データ（data/{userId}/travel.db・
# agent-sessions/{userId}/）とグローバル DB。誤ってコミットしないよう除外する。
data/*/
data/global.db*

# SQLite データ（seed で再生成可能）。
# 編集内容をバージョン管理したい場合はこの行を消してください。
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm
