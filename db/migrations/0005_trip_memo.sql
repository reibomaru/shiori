-- ============================================================
--  trip テーブルに memo カラムを追加する。
--  旅全体に紐づく単一の Markdown フリーテキストメモ（/memo ページ用）。
--
--  単純な列追加なのでテーブル再構築は不要。既存データは保持される。
-- ============================================================
BEGIN;

ALTER TABLE trip ADD COLUMN memo TEXT;

COMMIT;
