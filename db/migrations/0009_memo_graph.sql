-- ============================================================
--  memo_pages に graph カラムを追加する。
--
--  画像に「グラフ構造」（フローチャート・組織図・相関図・マインドマップ
--  などのノード＋エッジで表される図）が含まれる場合、OCR 抽出で
--  {"nodes":[...], "edges":[...]} という JSON を生成してここに保存する。
--  表示は React Flow（閲覧専用）で行う。棒/折れ線/円グラフなどの
--  「チャート」は従来どおり html カラムに保存するため、これとは別物。
-- ============================================================
BEGIN;

ALTER TABLE memo_pages ADD COLUMN graph TEXT; -- グラフ構造の JSON（{nodes, edges}）。無ければ NULL。

COMMIT;
