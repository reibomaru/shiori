-- ============================================================
--  領収書/請求書の元ファイル（expense_images）に元のファイル名を保持する。
--  一覧・フォーム・プレビューで「何のファイルか」を表示するために使う。
--  既存行は NULL（不明）。表示側はファイル名が無ければ種別ラベルで代替する。
-- ============================================================
BEGIN;

ALTER TABLE expense_images ADD COLUMN filename TEXT;

COMMIT;
